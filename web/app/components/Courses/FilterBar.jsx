import React, { StrictMode } from 'react';

const FilterBar = ({ filterByCategory }) => (
  <StrictMode>
    <div className="card">
      <h4 className="float-left text-primary">FILTERS</h4>
      <div className="btn btn-primary"/>
    </div>
  </StrictMode>
);

export default FilterBar;
